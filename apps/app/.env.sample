### TasteOS Web App Env (Phase 6.5)
###
### Copy this file to `.env.local` or `.env` and tweak as needed.
### VITE_API_BASE should point at the FastAPI backend base URL.
### In dev, you'll likely run FastAPI at http://localhost:8000
### and Vite at http://localhost:5173.
###
### The frontend fetch() calls always do `credentials: "include"`
### so make sure your backend in dev sets CORS / cookies accordingly.

VITE_API_BASE=http://localhost:8000
