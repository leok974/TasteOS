// Re-export design system components
export { default as tailwindPreset } from './tailwind-preset'

// CSS files are imported directly by consumers:
// import '@tasteos/design/tokens'
// import '@tasteos/design/fonts'
